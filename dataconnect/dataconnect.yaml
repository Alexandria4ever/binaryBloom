specVersion: "v1"
serviceId: "wb2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wb2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
